# V3.8.2 fixes

- The group is automatically stored as the main group when an admin opens `/panel` or uses `/setwelcome`.
- Added **Κύρια ομάδα** inside `/panel` for explicit re-linking.
- `/adminhelp` in private chat now checks admin status in the configured main group.
- `/rank` and other V3 private commands work after the group is connected.
- `/setwelcome` accepts media both by replying to a photo/video/GIF/document and by sending the command directly as the media caption.
- Existing verification records and database migrations are preserved.
