# V3.7 Safe Database & Welcome Update

## Περιλαμβάνει
- Μόνιμο verification στον πίνακα `verified_users`.
- Μη καταστροφικό migration από παλιές εγγραφές `members.verified`.
- Καμία εντολή `DROP TABLE`, καμία διαγραφή verification κατά το startup.
- `/verifyallknown` για όλα τα γνωστά μέλη της τρέχουσας ομάδας.
- Πλήρες `/setwelcome` με κείμενο ή reply σε κείμενο/φωτογραφία/video/GIF/αρχείο.

## /setwelcome
- `/setwelcome Καλώς ήρθες {mention}!`
- Reply σε μήνυμα ή πολυμέσο και γράψε `/setwelcome`
- `/setwelcome preview`
- `/setwelcome status`
- `/setwelcome on`
- `/setwelcome off`
- `/setwelcome reset`

Placeholders: `{mention}`, `{first_name}`, `{username}`, `{user_id}`, `{group}`

## Railway persistence
Η μεταβλητή `DATABASE_PATH` πρέπει να παραμένει `/data/secret_club.db` και το Railway Volume να είναι mounted στο `/data`.
