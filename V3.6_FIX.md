# V3.6 Manual Verification Fix

- Manual verify/unverify now creates or updates the member row for the current group.
- Dashboard, stats and lists use `verified_users` as the authoritative source.
- Existing bot functions are unchanged.
